{
    "rules": {
        ".read": "auth != null",
        "presence": {
            "$canvasId": {
                "$userId": {
                    ".write": "auth != null && auth.uid == $userId",
                    ".read": "auth != null"
                }
            }
        }
    }
}