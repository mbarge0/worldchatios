export default function Home() {
    return (
        <main className="flex min-h-screen items-center justify-center p-8">
            <div className="max-w-2xl text-center space-y-4">
                <h1 className="text-3xl font-bold">Foundry Core v2</h1>
                <p className="text-base text-gray-600">Reusable framework for structured, AI-assisted, multi-phase projects.</p>
                <p className="text-sm text-gray-500">Edit this landing page at <code>app/page.tsx</code>. Automation lives in <code>scripts/</code>; tools in <code>tools/</code>.
                </p>
            </div>
        </main>
    );
}