import { NextResponse } from 'next/server'

// Placeholder for AI chat endpoint (Module 4)
export async function POST() {
    return NextResponse.json(
        { message: 'Chat endpoint active - AI features coming in Module 4' },
        { status: 200 }
    )
}
