import { test } from '@playwright/test'

test.describe.skip('Text creation and security rules', () => {
    test('user can create text and edit it; rules enforced (emulator required)', async ({ page }) => {
        // TODO: implement when emulator-backed auth and seeding are wired
    })
})


