import { describe, it } from 'vitest'

// Skipped placeholder until emulator test deps are installed
describe.skip('Firestore security rules', () => {
    it('allows authed read/write on existing canvas and shapes', async () => { })
    it('blocks unauthenticated access', async () => { })
})


