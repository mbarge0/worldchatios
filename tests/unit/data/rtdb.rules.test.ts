import { describe, it } from 'vitest'

// Skipped placeholder until emulator test deps are installed
describe.skip('RTDB security rules', () => {
    it('allows same-user writes under presence/{canvasId}/{userId}', async () => { })
    it('blocks cross-user writes', async () => { })
})


